import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "org.bluelock.subscription-manager",
  appName: "Subscription Manager",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
